# Apartment Book – web app

This is the Next.js website. Setup, deployment and the mobile-app plan are documented in the root [README.md](../../README.md).

```bash
cp .env.example .env.local   # fill in your Supabase URL + anon key
npm run dev                  # from the repository root
```
